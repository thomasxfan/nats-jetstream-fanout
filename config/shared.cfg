accounts {
  SYS: {
    users = [
      { user: "sys", password: "sys" }
    ]
  }

  APP: {
    jetstream: enabled
    users = [
      { user: "user", password: "user" }
    ]
    mappings: {
      "user.messages.*": "p.user.messages.{{partition(100,1)}}"
    }
  }
}

system_account: SYS
